'use strict';

/**
 * autonivelante-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::autonivelante-project.autonivelante-project');
