'use strict';

/**
 * autonivelante-project controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::autonivelante-project.autonivelante-project');
