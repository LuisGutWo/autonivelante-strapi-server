'use strict';

/**
 * autonivelante-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::autonivelante-project.autonivelante-project');
